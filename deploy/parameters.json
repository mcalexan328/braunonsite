{
  "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentParameters.json#",
  "contentVersion": "1.0.0.0",
  "parameters": {
    "keyVaultName": {
      "value": "braunkey"  // The name of your Key Vault
    },
    "acrPasswordSecretName": {
      "value": "acr-password"  // The name of the secret in the Key Vault
    }
  }
}